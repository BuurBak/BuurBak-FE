import { MagnifyingGlassIcon } from "@heroicons/react/24/outline";
import { ChangeEventHandler } from "react";

type CardType = {
  label?: string;
};

const Card = ({ label, ...props }: CardType) => {

  return (
    <div className="">
      </div>
  )
};

export default Card;